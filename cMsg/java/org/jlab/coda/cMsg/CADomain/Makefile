# Generated by Carl Timmer
#

JAR_INSTALL = ./

JAVAC = javac

TOPDIR = ../../../../..
JARDIR = $(TOPDIR)/jar
JARS = $(JARDIR)/jca-2.1.5.jar

INSTALL  = $(TOPDIR)/install-sh -c
JAVAC_FLAGS = -classpath $(TOPDIR)/:$(JARS)

JAVA_OBJS = \
	CA.class

all: $(JAVA_OBJS)

clean: 
	rm -f core *~ *.class

# SUFFIX RULES

.SUFFIXES: .java .class

.java.class:
	$(JAVAC) $< $(JAVAC_FLAGS)
